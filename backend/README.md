# DermaSmart Backend

FastAPI backend for DermaSmart. Handles ML validation, inference, and Gemini integration.

## Setup
Run the start script from the root of the project:
```bash
./start-backend.sh
```
